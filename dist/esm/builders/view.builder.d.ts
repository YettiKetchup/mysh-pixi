import { Container } from '@pixi/display';
import { MaskData, Texture } from '@pixi/core';
import { Component, ComponentType, EntitiesCollection } from 'mysh';
import { ViewUnion } from './data/types';
import { PixiEntity } from '../core/entities';
export declare class ViewBuilder<T extends Container> {
    private _root;
    private _current;
    private _entity;
    protected get root(): T;
    protected set root(value: T);
    protected get current(): T;
    protected set current(value: T);
    protected get entity(): PixiEntity;
    protected set entity(value: PixiEntity);
    constructor(root: ViewUnion<T>, name?: string);
    isVisible(visible: boolean): ViewBuilder<T>;
    isMask(mask: Container | MaskData): ViewBuilder<T>;
    isInteractive(value: boolean): ViewBuilder<T>;
    withZIndex(index: number): ViewBuilder<T>;
    withName(value: string): ViewBuilder<T>;
    withPivot(x: number, y: number): ViewBuilder<T>;
    withRelativePivot(x: number, y: number): ViewBuilder<T>;
    withAnchor(x: number, y: number): ViewBuilder<T>;
    withTexture(texture: Texture | string): ViewBuilder<T>;
    withAlpha(value: number): ViewBuilder<T>;
    withTint(color: number): ViewBuilder<T>;
    withPosition(x: number, y: number): ViewBuilder<T>;
    withPositionX(x: number): ViewBuilder<T>;
    withPositionY(y: number): ViewBuilder<T>;
    withScale(x: number, y: number): ViewBuilder<T>;
    withSkew(x: number, y: number): ViewBuilder<T>;
    withWidth(width: number): ViewBuilder<T>;
    withHeight(height: number): ViewBuilder<T>;
    withAngle(value: number): ViewBuilder<T>;
    withNode<K extends Container>(node: ViewUnion<K>, name?: string): ViewBuilder<T>;
    withChildren(): ViewBuilder<T>;
    endChildren(): ViewBuilder<T>;
    withFactory(callback: (builder: ViewBuilder<T>, data: any) => void, data: any): ViewBuilder<T>;
    rootAsCurrent(): ViewBuilder<T>;
    asEntity(collection: EntitiesCollection, entity?: PixiEntity): ViewBuilder<T>;
    withComponent(component: Component | ComponentType<any>, isObservable?: boolean): ViewBuilder<T>;
    withComponents(component: Component[] | ComponentType<any>[], isObservable?: boolean): ViewBuilder<T>;
    build(): T;
    private exctractView;
}
